node_modules/
# Holds the SmartThings token / device id — must not be committed.
smartthings-config.json
# Runtime log written by the hidden daemon launcher.
*.log
# Electron build output (regenerated by `npm run electron:build` / electron-builder).
dist-electron/
release/
# Generated app icons (regenerated by scripts/build-electron.mjs).
build/
